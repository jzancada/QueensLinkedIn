# Entorno virtual
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Stage dumps from queens.dump_stages
out/

# IDE. .vscode/ is shared on purpose (interpreter, pytest, launch configs);
# only the per-user files inside it are ignored.
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
